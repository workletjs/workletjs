---
keyword: ZoomControlPage
---

A control with 2 buttons, one for zoom in and one for zoom out. This control is one of the default controls of a map. To style this control use css selectors `.ol-zoom-in` and `.ol-zoom-out`.

## Examples

### Zoom Control

This example shows how to add a zoom control to a map.

{{ NgDocActions.demo("WolZoomControlExampleComponent") }}

{{ NgDocApi.api("packages/ngx-openlayers/control/zoom/zoom-control.component.ts#WolZoomControlComponent") }}
