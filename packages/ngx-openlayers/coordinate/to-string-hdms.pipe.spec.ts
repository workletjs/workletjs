import { WolToStringHDMSPipe } from './to-string-hdms.pipe';

describe('WolToStringHDMSPipe', () => {
  it('create an instance', () => {
    const pipe = new WolToStringHDMSPipe();
    expect(pipe).toBeTruthy();
  });
});
