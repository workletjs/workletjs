import { WolToStringXYPipe } from './to-string-xy.pipe';

describe('WolToStringXYPipe', () => {
  it('create an instance', () => {
    const pipe = new WolToStringXYPipe();
    expect(pipe).toBeTruthy();
  });
});
