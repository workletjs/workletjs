import { WolTransformExtentPipe } from './transform-extent.pipe';

describe('WolTransformExtentPipe', () => {
  it('create an instance', () => {
    const pipe = new WolTransformExtentPipe();
    expect(pipe).toBeTruthy();
  });
});
