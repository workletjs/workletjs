import { WolTransformPipe } from './transform.pipe';

describe('WolTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new WolTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
