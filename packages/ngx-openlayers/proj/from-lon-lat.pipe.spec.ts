import { WolFromLonLatPipe } from './from-lon-lat.pipe';

describe('WolFromLonLatPipe', () => {
  it('create an instance', () => {
    const pipe = new WolFromLonLatPipe();
    expect(pipe).toBeTruthy();
  });
});
